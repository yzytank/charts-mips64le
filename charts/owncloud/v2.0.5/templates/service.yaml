apiVersion: v1
kind: Service
metadata:
  name: {{ template "owncloud.fullname" . }}
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: {{ template "owncloud.name" . }}
    release: "{{ .Release.Name }}"

